import xlwt
import sys

def main() -> None:
    inFile = sys.argv[1]
    outFile = sys.argv[2]
    numStudents
    
    inputF = open(inFile, "r")
    for line in inputF.readlines():
        tempStr = line.rstrip("\n")
        tempList = tempStr.spli(",")
        name = tempList[0]
        nameList.append(name)
    inputF.close()

    nameList.pop()

    wb = Workbook()

    for i in range(1, numStudents+1):
        #
    

    


if __name__ == "__main__":
    main()
